{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run QEMU",
            "type": "shell",
            "command": "make qemu-gdb",  // 使用 make 命令启动 QEMU
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        },
        {
            "label": "Stop QEMU",
            "type": "shell",
            "command": "pkill qemu-system-riscv64",  // 停止所有 QEMU 实例
            "problemMatcher": []
        }
    ]
}
